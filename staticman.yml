comments:
  allowedFields: ["name", "message"]
  branch: "master"
  commitMessage: "There's been a new message left!"
  filename: "entry{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: false
  name: "stephaniescodingadventures"
  path: "_data/comments/{options.slug}"
  filename: "{@id}"
  requiredFields: ["name", "message"]
  transforms:
    email: md5
